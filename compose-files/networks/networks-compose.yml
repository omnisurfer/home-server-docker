# NOTE: If network 'traefik-network' is not present, run the below command to make it
# docker network create traefik-network

networks:
  proxy:
    name: proxy
  traefik-network:
    external: true  
  internal: